@model IEnumerable<Photos.Models.ValidationNbrDecimal>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nb1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nb2)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nb1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nb2)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ValidationNbrDecimalID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ValidationNbrDecimalID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ValidationNbrDecimalID })
        </td>
    </tr>
}

</table>
